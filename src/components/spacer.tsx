import React from "react";

import * as util_styles from "src/styles/utils.module.scss";

const Spacer: React.FC = (props) =>
    <div className={util_styles.spacer}>
    </div>

export default Spacer;
